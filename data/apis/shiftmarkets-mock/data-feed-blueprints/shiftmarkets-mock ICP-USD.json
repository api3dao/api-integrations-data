{
  "name": "ICP/USD",
  "path": "v1/price",
  "parameters": {
    "symbols": "ICP/USD"
  },
  "parser": "(response) => {\n          return response.find((item) => item.symbol_id === 'ICP/USD').price\n        }"
}
