{
  "name": "RON/USD",
  "path": "v1/price",
  "parameters": {
    "symbols": "RON/USD"
  },
  "parser": "(response) => {\n          return response.find((item) => item.symbol_id === 'RON/USD').price\n        }"
}
