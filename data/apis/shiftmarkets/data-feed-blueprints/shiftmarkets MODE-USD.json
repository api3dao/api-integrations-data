{
  "name": "MODE/USD",
  "path": "v1/price",
  "parameters": {
    "symbols": "MODE/USD"
  },
  "parser": "(response) => {\n          return response.find((item) => item.symbol_id === 'MODE/USD').price\n        }"
}
